# Repository Guidelines

## 计划模式约束
总是把在计划模式下创建的计划保存到 <project_folder>/docs/plans/ 目录下，计划文件名为 <feat-plan-YYYYMMDD>.md ,计划模式下的所有操作都必须在计划中记录，不能直接修改数据库或配置文件。

## 协作与执行原则

使用简体中文沟通。先阅读相关实现，再修改；优先复用已有代码和测试，以最小必要改动完成已确认需求。重大假设必须说明，普通实现细节自行解决。

用户已授权实施确认过的计划，不重复请求同类授权。缺少数据库连接或终端配置时，继续完成不依赖这些信息的开发；不得编造连接信息或宣称实机验证通过。

保留用户已有改动。不得修改源项目 cfquant 的工作区，不得替换其他应用的配置、数据库或 QMT 模型。每次交付说明实际变更、验证结果和未完成事项。

## 项目定位与范围

pfor-qmt 是基于 cfquant 派生的行情、指数查询和历史数据库工具。

支持一个大 QMT 行情源。2026-09-17 用户确认扩展为股票、行业概念、指数、期货、期权、场内基金、债券七类，优先完成期货和期权；覆盖终端实际提供的国内品种。提供实时行情、日线、1/5 分钟线、当前成员、PostgreSQL 存储及 CSV/Parquet 导出。

保留 QMT 合约代码大小写和交易所后缀，区分普通合约、连续合约及组合合约。期权标的、行权价、到期日等仅使用终端资料，不从名称猜测。行业概念属于板块与成员关系，不伪装为可交易证券或有独立K线的指数。期货夜盘时间与交易日分别保存，无法确认交易日归属时标记待核验，不套用股票交易时段。

2026-09-17 用户确认增加 Tushare 期货数据源与多账号管理：目录、期货日历、主力映射、日线及具体月份合约的 1/5 分钟历史。复用统一目录、任务、K线表、查询和导出，不自动切源、混源或轮换 Token。主力连续分钟拼接、Tushare 实时行情及研究接口不在本次范围。

2026-09-18 用户授权扩展 Tushare 周/月线、15/30/60分钟、独立交易日历与主力映射同步、仓单及每日成交持仓排名。均复用现有任务、账号和schema；非K线资料按自身维度建表。2000积分不等于分钟授权；Tick无API，不构造下载入口或模拟数据。连续分钟仍通过映射选择实际月份合约，不合成连续序列。

第一期不提供交易、资金持仓查询、自动多源路由、LTtx 高级模式、LITE 模式、Level2、千档、财务研究、公式、历史指数成分与权重、自动登录或在线更新。新需求不得因上游已有代码而自动纳入。

## 源码来源与许可证

固定上游提交为 5baa4daf8dab01fb415afdd45a72cd254cea042f。从该提交提取经过筛选的受版本控制文件，不复制本地工作区、忽略文件、运行数据和凭据。

保留 MIT 许可证、Copyright (c) 2026 tao 及适用版权声明。记录实际使用的第三方代码、资源及许可证。产品文案注明派生关系，不冒充上游官方产品。

UPSTREAM.md 记录来源、路径映射和修改理由；COMPATIBILITY.md 记录保留、修改、移除、新增接口及验证状态。后续上游同步采用选择性移植和回归测试。

## 目录与工程边界

pfor_qmt/ 存放 SDK、服务及业务模块；qmt_scripts/ 存放终端入口；web_dashboard/ 存放静态界面；tests/、tools/、docs/ 分别存放测试、工具和文档。

runtime/、log/、导出文件及本地连接配置不提交。AGENTS.md、迁移脚本、来源说明、兼容矩阵和依赖锁定文件必须提交。

统一使用根目录 config.toml 管理本地配置，提交无凭据的 config.example.toml。优先级为显式命令行参数、环境变量、TOML、默认值；网页设置写回同一文件。QMT 内嵌端只接收部署器生成的行情参数，不复制数据库凭据或 API Key。

主程序使用 Python 3.12；QMT 内嵌代码保持 Python 3.6 兼容和必要的 GBK 编码。普通源码使用 UTF-8。Python 使用四空格缩进，前端沿用两空格和现有风格。

复用上游成熟实现，不为未来需求预建复杂抽象。迁移、命名调整、缺陷修复和新增功能分阶段提交，避免混入整库格式化。

## 行情桥与命名隔离

使用 pfor_qmt 包、pfor-qmt 命令、独立管道和模型、独立运行目录及数据库 schema。

命名变更逐项检查导入、配置、资源、模型识别及清理逻辑，不做全局字符串替换。保留的协议标记须有记录。

SDK、HTTP 和桥端仅开放明确允许的行情操作，包括订阅和历史下载。不得初始化交易回调或暴露交易 action；不能仅通过隐藏网页入口裁剪交易功能。

部署器只能管理 pfor-qmt 自有模型和文件，不得清理、停用或覆盖既有 cfquant 模型。

## 数据与任务约定

2026-09-20 用户授权QMT交易日历与主力映射接入。当前主力必须来自终端明确返回，采集快照与历史逐日映射分别保存，缺交易日保留NULL。完整日历、观察到的K线日期及未知日期须区分，旧QMT日历不自动升级。能力检测不因函数存在或空响应宣称可用、无权限；不得新建MiniQMT连接或借用Tushare数据填补。

2026-09-20 历史行情汇总必须使用可分页表格，不用无边界指标块堆叠；主力映射和结算参数展示名称时只使用同源证券目录，代码作为辅助标识，缺名称不得猜测。图表、列表和详情保留原始精度与NULL。

2026-09-20 用户授权在适合的列表增加删除能力。配置、任务和导出记录采用可恢复回收站；配置恢复后保持停用，活动任务及配置依赖须校验，旧任务证据与行情文件保留。默认工作列表排除回收站，质量核验不得因此丢失未解决问题。测试删除流程仅使用隔离schema，不删除用户业务记录作为验收。

2026-09-18 用户授权新增Tushare结算参数fut_settle与主要品种交易周报fut_weekly_detail，复用现有资料、任务、维护、核验和导出链路。结算费率和手续费保留上游原值，不猜测百分比基准；周报金额亿元精确换算为元并保留原值，保留原始week（含旧非补零格式）与week_date，不假定为ISO周。周报不能按逐日行情规则判缺口。

PostgreSQL 保存不复权行情及可获得的复权因子。明确区分 QMT 数据与数据库数据，保留字段单位、数值精度、时间语义和来源。

按稳定合约 ID、数据来源、周期和行情时间去重并更新；账号不是数据来源维度。原始代码通过来源映射保留，资料不足不猜测跨源合并。交易时间按 Asia/Shanghai 解释。空值不补零，缺行情不伪造，当前成分快照不冒充历史成分。

Tushare 凭据只保存在本地统一配置或环境变量中，API 不回传 Token。任务固定账号与端点，不受默认账号变化影响。日线成交额万元转换为元，分钟成交额保留元，Decimal/NUMERIC 保留精度；旧 QMT 单位未核验前不改写。无明确交易日的分钟保留空值和待核验状态。

周/月线周期标签和计算截至日期分别保存，未结束周期以not_published标记等待周期结束，不用日线逐日覆盖规则校验。仓单保留单位和仓库/等级/年度等明细维度；持仓排名的未上榜字段保留NULL，不计算虚假的名次。闭市日不能参与自动交易日调度；INE持仓数据按官方SHFE入口读取并保留返回交易所。

下载完成必须以实际读取、校验和入库结果为依据。请求返回、拿到订阅号或读到空表均不等于业务成功。旧接口回退必须覆盖全部请求证券。

任务持久化检查点，支持重启恢复、取消和失败重试。同一行情源只执行一个补数任务；取消只停止后续处理，不能声称撤销已经发送的终端请求。

迁移和业务写入限定在 pfor_qmt schema。凭据不进入代码、日志或导出文件；自动化测试使用隔离测试 schema，不使用用户业务数据。

## 验证与交付

2026-09-18 全链路整改约定：执行状态与数据质量分别记录；partial 显示为部分完成，不再统称待核验。同一合约分块的坏数据不得入库，独立合约继续；认证、连接和数据库故障不得被误报为普通缺口。重试创建关联任务，只处理未完成范围，不覆盖原任务证据。

自动维护覆盖已接入资料，但仅作用于用户明确保存的维护范围；不扩大证券选择，不自动切源或轮换账号。权限错误不自动重试，网络请求最多重试三次，业务补数必须有次数和时间边界。站内健康与日志面板报告可行动异常；任务事件保留90天，脱敏异常样本保留30天，不清除行情数据或未解决问题的定位信息。

筛选与采集支持多选、全选、跨页保留及取消还原；全选匹配结果须按同一数据库快照读取，超限明确报错，不静默截断。批量写入先完整校验，再原子创建原有类型任务，保持账号绑定和取消恢复语义；来源与采集账号保持单选，不跨来源拼接结果。

快捷日期按 Asia/Shanghai 计算并包含结束日；月、年按日历回退并处理月末。记录详情保留单位、来源、NULL与原始字段；多证券多周期表格必须有明确标识，图表只展示一个证券和周期。浏览器测试覆盖筛选重置、跨页全选、批量确认取消及窄视口详情。

迁移接口使用相同模拟 QMT 输入与上游对比，验证参数、结果、字段、回调及错误行为；所有预期差异写入兼容矩阵。

覆盖交易请求拒绝、命名与部署隔离、订阅退订、下载回退、重复入库、断线恢复、任务取消、调度去重和文件导出一致性。

Web 修改执行浏览器流程与桌面、移动视口检查。构建产物必须包含许可和必需资源，不得包含本地配置及运行数据。

分别报告离线通过、实机通过、失败和未验证；不得跳过或弱化失败测试以满足验收。
