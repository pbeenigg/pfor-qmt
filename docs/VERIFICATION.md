# 验证记录

## 只读核验与运行闭环（2026-09-18）

最终完整回归`298 passed in 278.47s`，0失败、0跳过。使用隔离schema、模拟来源和Chromium；新增只读核验去重、离线账号、取消/重启、周/月修订和同截至日冲突、节假日周期结束、负数量、分钟可疑间隔、完整日历保存、关联剩余缺口补数、权限/未结束周期隔离、目录证据和数据库断开日志。跨日回放涵盖周五、周末、停机跨过周一后周二恢复，去重与7条日线数据一致。

首轮完整回归296通过、1失败，定位到交易所切换后已打开的产品选择框不刷新异步选项；在共用选择器MutationObserver修复，并新增受控延迟响应回归。跨日测试最初使用未来日期被既有采集限制正确拦截，改为已结束历史日期回放，未放宽业务限制或断言。

升级前备份`runtime/backups/schema6-before-verification-20260918-182610.dump`（20,520,142字节），归档目录检查通过；schema6→7前后18张原表记录数与全部原字段哈希一致，原82任务不变。报告`runtime/backups/schema7-verification-20260918-182610.json`。仅重启本项目服务，HTTP/WebSocket就绪，无终端模型或其他数据库变更。

实库新增一个只读核验任务`1f99ee07-c2c6-4add-a2d1-e6a2f3360153`，核验34行、succeeded、规则coverage-v2、6条事件。原任务`dc18ab8f-a0bb-4608-9e6c-71fea78f9390`未改写，141,045条K线全表哈希不变；未发出行情下载。结果`output/live-verification-20260918.json`。完整分钟时段、夜盘及资料发布时间的外部证据仍不足，参见QUALITY_VERIFICATION.md。

实库浏览器验证任务筛选、核验详情、分块依据及运行日志通过，桌面1440×980和手机390×844无页面或详情横向溢出、无脚本错误；截图`output/playwright/live-verification-*.png`。验收脚本首次在筛选响应到达前点击宽泛定位器失败，改为等待单条结果并按任务ID定位后通过，未修改产品断言或扩大采集范围。

最后将分块依据改为中文字段与可疑间隔表格，原始记录保留在展开区；相关浏览器专项`1 passed in 8.48s`。依赖、脚本语法和分发资源检查通过，产物含schema7与核验规则，不包含本地配置及运行数据。

收尾补充“已取消补数不自动重建”保护，核验与任务可靠性专项`40 passed in 33.72s`；不把此结果改称新一轮完整回归。真实A2611.DCE五分钟窗口的ft_mins探测仍返回permission，未入库，分钟实机验收继续阻塞于账号权限。

## 新鲜度与备份恢复（2026-09-18）

完整回归`284 passed in 254.40s`，0失败、0跳过。使用本机PostgreSQL随机`pfor_qmt_test_*`隔离schema、模拟行情源、真实管道与Chromium；未向业务表写测试行情。新增调度截止、周末缺日、合约存续期、逐对象分页、来源隔离、显式维护、日历/映射/仓单/排名、SDK/健康同口径检查。浏览器覆盖51对象跨页、搜索重置、详情及1440×980/390×844视口，截图`output/playwright/reliability-*.png`。

原schema5备份在无网络、无发布端口、临时内存数据卷的PostgreSQL16容器完整恢复；15张原表计数与备份清单相等，再升级schema6，原字段行数与哈希保持一致，63条completed按预期转succeeded。临时容器已移除，报告`output/backup-verification-20260918.json`；工具`tools/verify_backup.py`可重跑。未向业务库恢复备份。

业务库只读新鲜度首屏读取50条、共2015维护对象，约0.03秒；此性能数字仅对应当前样本。业务PostgreSQL数据目录所在文件系统实测约848GiB可用；这是一时点检查，应用仍将远端/容器卷持续监控列为未验证。

当前共享工作区另有监听地址配置改动。验证发现浏览器Origin含HTTP端口，不能与WebSocket端口直接相等比较；保留该配置并按HTTP端口修正来源检查，原推送测试现带真实浏览器Origin验证。分钟逐时段连续性、夜盘归属、资料官方发布时点与真实跨交易日长跑仍未验收；新鲜度达到维护目标不代表这些质量检查通过。本轮无schema迁移、不扩大维护范围、不自动重试旧任务。

17:23重启本项目服务后，实际SDK新鲜度接口、Chromium搜索/详情、桌面/手机视口和WebSocket连接通过，浏览器脚本错误为0；前后任务均82个。截图`output/playwright/live-freshness-*.png`。构建、必需资源/许可证检查、pip依赖和全部自有JS语法检查通过；本次变更实际凭据匹配为0。测试进程补齐缺失Windows环境变量，仅对子进程生效，未修改系统配置。

## 全链路执行与质量（2026-09-18）

最终全量回归`279 passed in 259.03s`（含8个Chromium浏览器用例），0失败、0跳过；使用本机PostgreSQL 16的随机`pfor_qmt_test_*`隔离schema，未向业务表写入测试数据。命令：设置PFOR_QMT_TEST_DSN、PFOR_QMT_BROWSER_TEST=1后运行`.venv\Scripts\python -X utf8 -m pytest -q --tb=short`。包含账号缺失与端点变更应阻塞、不能误报坏行情的补充回归。

新增故障注入覆盖OHLC区间/部分为空、同时间冲突、负数量但允许负价格、坏合约隔离、权限仅阻塞对应接口、上市区间、原子回滚、重复写统计、租约连接丢失、关联定向重试、部分修复仍保留其他异常、日志脱敏与游标/清理、目录和四类资料维护、次数限制和手动任务不自动重跑。浏览器走通详情→分块→定向重试、日志过滤/详情、保存/停用维护，检查1440×980与390×844截图和溢出；截图`output/playwright/reliability-*.png`。

回归期间按新契约修改旧测试预期：completed→succeeded、部分已入库任务→partial、前置条件缺失→blocked、重试返回子任务；只返回close的测试样本保留NULL并要求OHLC_INCOMPLETE，不再声明整批校验通过。未移除精度、重复入库、取消、导出或隔离断言。

升级前备份`runtime/backups/schema5-before-reliability-20260918-153824.dump`（20,514,020字节），pg_restore --list验证成功，未进行恢复演练。schema5→6前后15张原业务表记录数及原字段哈希一致：bars 141,045、securities 90,483、contract_mappings 72,807、futures_warehouse_receipts 321,708、futures_holdings 112,198、jobs 82；jobs比较排除有意改名的state，另断言63个completed转换为succeeded，10个failed和9个partial保持。对比结果保存在`runtime/backups/schema6-reliability-verification.json`。

服务经pfor.ps1重启，8766 HTTP/WebSocket就绪，真实SDK读取health/jobs/events/maintenance通过。没有新建采集或自动维护任务，已有8个自动更新数据集保留；总任务仍82。QMT仍报告交易日历连接问题，不能视为恢复；真实分钟权限、夜盘连续性、逐品种发布时效和跨交易日维护长跑均未验收。旧任务没有补造事件，新增日志从升级后的任务开始。

构建、依赖、JS语法及git diff检查通过；wheel/sdist含schema6、operations.js和许可证，不含本地配置或运行数据。已检查本次变更不含实际API Key、Token或DSN。stale按资料时效自动判定尚未实现，见RELIABILITY_PLAN.md的明确边界。

## 多选、快捷日期与详情（2026-09-18）

完整回归：`253 passed in 197.81s`，无失败、无跳过。独立postgres:16-alpine容器55434、随机测试schema及Chromium；设置PFOR_QMT_TEST_DSN和PFOR_QMT_BROWSER_TEST=1后运行`.venv\Scripts\python -m pytest -q --tb=short`。测试容器已移除，未触碰用户PostgreSQL。

新增验证包括63项跨50条分页全选、10000项超限拒绝、选择窗口取消还原、目录多交易所同步、闰年/月末快捷日期、数据集周期交集、整批参数/权限校验、无部分创建、断点恢复、资料分类型查询、跨200条任务筛选分页、双格式导出和详情切换。首次完整回归252通过、1失败，定位到reset事件微任务早于浏览器默认表单重置；改为下一任务读取筛选后完整253项通过，保留原断言。

业务服务通过pfor.ps1重启并监听8766，HTTP/WebSocket就绪。本次无数据库迁移，schema仍为5；无采集任务运行时重启。原失败任务4d9435b8整条记录排序哈希前后相同，状态failed、检查点1669保持；未修改cfquant或终端模型。

真实库SDK批量分页与原单项接口、直接SQL比对：09-14至09-20两所日历14条、A.DCE映射5条、DCE/A及SHFE/CU仓单126条、DCE/A2611排名29条；A2611.DCE的08-01至09-17周线8条/月线2条，批量与单项一致。六组CSV/Parquet共12份文件逐字段比较通过，保留十进制、NULL、原始字段和口径说明。CSV时间空格与API的T分隔按同一ISO时刻比较，不改写导出格式。验证脚本首次时间字符串比较不等，修正比较方式后通过；前后共产生18个小样本导出任务，未触发新的历史采集。

真实Chromium检查1440x980、390x844、768x1024：两所日历14条、09-17仓单63条、记录详情与历史库多周期10条；无页面脚本错误、横向布局溢出或详情容器溢出。初次实机脚本使用一周日期却断言一天仓单数量，已修正日期并验证；未减少结果。截图为`output/playwright/live-batch-*.png`，比较结果为`output/live-acceptance/batch-20260918/comparison.json`，均不提交。

权限检测首次目录接口出现network，依赖合约样本的检测标为unverified；重试后目录、日历、日线、映射、周/月线、仓单、排名均available，分钟permission、Tick unsupported。此次实机验证覆盖已入库数据批量查询及导出；批量采集、取消与恢复使用隔离模拟源验证，不宣称真实分钟或全市场覆盖通过。

wheel/sdist构建、资源/许可证检查、JS语法、git diff --check及pip check通过；新增controls.js包含于两种分发包。本地配置、运行数据、API Key、Token及数据库连接未进入产物。执行规范、API、实施计划和兼容矩阵同步更新。

## Tushare 期货资料扩展（2026-09-18）

离线完整回归：`239 passed in 160.29s`，无失败、无跳过。使用本轮独立postgres:16-alpine容器55434、随机测试schema和Chromium，启用PFOR_QMT_TEST_DSN、PFOR_QMT_BROWSER_TEST=1后运行`.venv\Scripts\python -m pytest -q --tb=short`。覆盖新增周期、精度、截至日回退保护、接口截断细分、仓单多维唯一键、闭市过滤、非空快照替换、取消恢复、重试、事务回滚、分页、共享任务和文件导出。首次回归暴露旧空行情提示不兼容，已恢复原提示再完整通过；没有删除断言。

业务库停服备份为`runtime/backups/before-futures-ext-v5-stopped-20260918-110135.dump`，15,305,791字节，pg_restore清单验证通过。schema4迁移至5前后137,687条原行情所有既有字段（包括updated_at、ID、NUMERIC）的排序哈希及数量一致；原大任务全记录也一致。证券目录仍为QMT 79,208条、Tushare 11,275条。未修改cfquant工作区、QMT模型或其他数据库。

真实小样本，均经原任务链路入库：

| 数据 | 样本与范围 | 结果 |
| --- | --- | --- |
| 日历 | CFFEX/SHFE/DCE/CZCE/INE/GFEX，09-14至09-20 | 各7条，completed，包含开休市及前交易日 |
| 主力映射 | A.DCE，09-14至09-17 | 4条，completed |
| 仓单 | DCE/A，09-17 | 21条，completed |
| 仓单 | SHFE/CU，09-17 | 42条，completed；铜与铜(BC)按上游名称独立保留 |
| 持仓排名 | DCE/A2611，09-17 | 29条，completed，未上榜字段保留NULL |
| 周/月线 | A2611.DCE，08-01至09-17 | 8周+2月，partial；当前周/月未结束，计算截至日保留09-17 |
| 能源中心持仓 | INE/SC，09-17，官方SHFE入口 | 0条，partial，不计为通过 |

仓单首次实源检测发现同一CU代码和仓库同时包含铜与铜(BC)，补入上游产品名称维度并增加回归，未猜测跨交易所重分类。新版服务加载后重新检测：目录、日历、日线、主力映射、周线、月线、仓单和持仓排名样本可用；分钟仍为permission，Tick为unsupported。2000积分不代表分钟权限。

七组样本（SHFE日历、A映射、两所仓单、A2611排名、周线、月线）均验证SDK每页3条与直接SQL一致；14个CSV/Parquet导出任务完成，所有字段、Decimal、NULL、日期、布尔及原始source_fields逐项一致，口径元数据匹配、CSV含UTF-8 BOM。结果为`output/live-acceptance/futures-extensions-20260918/comparison.json`，文件不含凭据；资料查询和导出只依赖数据库。

隔离浏览器覆盖同步、任务完成、下载文件、主力映射跳转和新增周期。实机浏览器另检查六类查询流程、合约选择、SDK价格匹配和权限禁用；1440x980桌面与390x844手机无页面脚本错误或稳定布局横向溢出，截图`output/playwright/live-futures-*.png`。页面显示计算截至日期及未结束周期，资料单位与K线单位分离。

用户在开发期间启动的全市场日线任务4d9435b8保留，首次重启从1639继续执行，随后在1669/2176因SCTAS.INE日线缺少全部OHLC字段而失败。只读复查该区间返回113条，全部OHLC为NULL；没有补零、跳过后冒充完成或重置检查点，任务已处理行数140,736保留。最终服务重启未改写该失败任务。

仍未验收：真实分钟数据及夜盘交易日、Tick文件导入、INE原油排名覆盖、全部产品资料覆盖、实际19:00长期调度。新资料仅手动同步；K线数据集保留原定时更新。QMT历史故障不在本次宣称修复范围。

## Tushare 故障修复（2026-09-18）

目录任务57893c97、44cf53de均在5/12中断：实际fut_basic大商所类型2返回52条，包含L_F、L_FL、PP_F、PP_FL、V_F、V_FL六个合法月均价主力/连续代码，旧校验误拒绝下划线。现保留市场及字符边界并兼容这些代码；非法记录错误附市场、类型及行号，不回显原始响应。

6fa116fa混合周期回补已成功写入34条日线，随后ft_mins返回40203，消息明确表示权限不足。修复每个成功分块事务保存result.rows；旧任务缺少统计时按coverage还原。新增本次回补周期选择及SDK/API可选periods子集，不修改数据集周期、账号、端点、定时配置或原失败任务。分钟拒绝仍为failed，失败块不推进检查点。

离线完整回归：`208 passed in 124.16s`，无失败、无跳过；独立postgres:16-alpine测试容器端口55434，每项使用隔离测试schema，启用PFOR_QMT_TEST_DSN与PFOR_QMT_BROWSER_TEST=1。新增代码兼容/拒绝边界、目录中途失败续跑、已入库统计持久化、旧任务统计、日线子集与原周期保留、SDK兼容和分钟权限浏览器流程。首次完整回归207通过、1项旧浏览器账号表单用例失败；补上登录弹窗关闭等待后完整通过，未删除断言。

实机更新前备份`runtime/backups/before-tushare-fix-20260918-092935.dump`并核验pg_restore清单，随后重启本项目服务。最近目录任务从检查点5续跑至12，完成11,275条Tushare合约资料；QMT目录仍为79,208条。新日线任务dc18ab8f对A2611.DCE回补2026-08-01至2026-09-17，完成34条（实际交易日2026-08-03至09-17），未确认缺口为空；行情表仍为34条，无重复新增。原数据集及6fa116fa、44cf53de记录保留。

当前账号分别检测：合约目录、期货日历、日线、主力映射可用；分钟权限拒绝。CSV任务d0b21cca、Parquet任务39407c7b均完成34条，与PostgreSQL及SDK全部字段核对一致；CSV含UTF-8 BOM、十进制精度保持、口径金额为元。初次比较误将CSV时间空格分隔与API的T分隔当作差异，改为解析带时区时间比较后通过，未修改导出格式。

Chromium隔离及实机页面均检查1440px桌面、390px手机。实机任务页展示12/12批次和旧任务已入库34行，权限检测后分钟禁用，历史页精确选择A2611.DCE并显示34条，与SDK价格一致；无页面脚本错误或稳定布局横向溢出。实机检查脚本最初误假设A2611模糊搜索只有一条，以及在resize布局前立即断言；改为精确合约定位并等待布局完成后通过。截图在`output/playwright/live-tushare-fix-*.png`；未向业务库注入模拟数据。

构建wheel/sdist及tools/verify_packages.py通过，必需资源与许可齐全，无本地配置、Token及运行数据。分钟数据采集、夜盘归属、全部品种历史覆盖与实际19:00长期调度未通过实机验收；本次不声称修复QMT历史连接。接口能力缓存仍只保存在服务内存，重启后可在设置页重新检测。

## Tushare 期货接入（2026-09-17）

完整回归197项通过，无跳过、无失败。命令：设置隔离`PFOR_QMT_TEST_DSN`与`PFOR_QMT_BROWSER_TEST=1`后运行`.venv\Scripts\python -m pytest -q`。测试使用临时postgres:16-alpine容器的独立测试schema及合成Tushare响应，不写用户业务行情。

新增覆盖：公开HTTP协议、Token及错误脱敏、多账号环境覆盖、共享Token限流、达到返回上限时分块、日线万元到元精确转换、分钟交易日空值、来源同表不覆盖、合约身份、v3迁移保值、账号与端点固定、取消和检查点恢复、19:00调度去重、QMT请求阻塞时Tushare独立执行、主力映射及CSV/Parquet一致性。

Chromium验证账号保存/权限检测、目录同步、搜索选择、新建数据集、下载、历史图表及文件导出，包含1440px桌面与390px移动视口，无页面脚本错误或页面横向溢出。新增异步响应序号，避免旧请求覆盖新来源和新任务结果；进入下载页前固定数据集选择。截图位于忽略目录output/playwright/tushare-*.png。

业务库先以pg_dump备份至`runtime/backups/before-tushare-v4.dump`，再迁移schema至4并重启8766服务。迁移前后均为79,208条证券资料、0条K线、6个数据集、13个任务；原目录均为qmt，稳定ID无缺失。SDK实际HTTP读取目录数量一致。运行中网页已在桌面/移动视口复核，未写入演示账号、凭据或合成行情。

标准隔离构建`python -m build`及`tools/verify_packages.py`通过，wheel/sdist包含新增模块、迁移、静态资源和许可证，无本地配置或运行数据。初次`--no-isolation`因开发环境未安装setuptools失败，改用pyproject声明的固定构建依赖后成功；未修改运行依赖。

真实Tushare账号尚未配置。官方HTTPS端点可达，但真实Token认证、积分/分钟权限、各交易所历史覆盖、夜盘交易日及实际19:00长期调度仍未验收；模拟接口通过不代表这些项目通过。原QMT历史故障不在本轮宣称修复。

日期：2026-09-17。主程序环境：Windows、CPython 3.12.10。下列结果来自本项目，不使用原cfquant测试数量充当本项目结果。

## 自选合约验收工具

工具新增`--codes`及`--period`，支持最多10个证券/合约的日线与单交易日1/5分钟线。保留默认三证券、显式下载开关、活动任务拒绝及未来日期限制。只读诊断按代码保存结果并明确诊断周期仍为日线；分钟数据即使文件比对一致，也不会将partial升级为通过。

14项工具单元测试和4项隔离PostgreSQL/HTTP验收测试通过。后者包括原三证券和期货/期权的三个周期，实际执行两次回补、SDK分页、数据库、CSV/Parquet比较，验证十进制精度、持仓量、结算及前一会话时间与显式交易日。测试仅运行相关范围，未把上一轮168项结果改称本轮完整回归。

18:52实机只读调用自选沪铜及HO股指期权成功，报告仍显示本地日线与交易日历为空。另对沪深各20个期权样本读取快照均为空；终端自带包装函数的接口参数与在用桥一致。没有新增实机下载、没有以模拟数据填充历史库，终端日K线及权限仍需外部核查。

## 七类资产扩展

完整结果：`168 passed in 54.02s`，0失败、0跳过。使用隔离PostgreSQL14及Chromium，真实业务库未写入模拟行情。新增多资产标识、批量资料、期权专用详情、分类树、schema3迁移、旧ETF兼容、板块快照、夜盘交易日及新增行情字段的端到端测试。

复核修正期权行权价末尾被误判为连续合约的问题，已校正业务库19,908条合约子类；原始代码、名称和资料保持不变。空板块刷新现在保留旧快照并报错；相同目录任务去重，扩展范围的活动请求明确拒绝。板块资料使用已有八路并发，逐板块提交检查点，服务重启后同一目录任务继续执行。

实机证券目录79,208条全部逐页核对通过：QMT发现集合与数据库代码/类别/子类一致，API与数据库代码/名称/类别/子类一致。报告：`output/live-acceptance/multi-asset-catalog-20260917.json`。商品及金融期权缺失字段仍为空，不根据合约名称补造资料。

第二个目录任务`03fd4cc0-6462-40d8-9e97-b64c6b0a6926`完成51,045/51,045、missing为空，其中48,974条证券及2,071个板块。行业782、概念1,289；任务发现路径、数据库、全部API分页及最新非空成员快照逐项一致。359个板块引用代码不在当前证券目录，抽查10个QMT详情均无名称；网页明确标注资料未收录，未丢弃成员或伪造名称。目录任务的missing为空不表示每个板块成员都能取得资料或行情。

真实Chromium检查期货/期权中文与代码搜索、复选、详情、普通合约筛选结果、快照及行业成员填入数据集；1440×980和390×844无页面错误或页面横向溢出。行业LED样本32个成员与QMT集合相等。截图为`output/playwright/live-multi-*.png`，使用真实资料，不含凭据。

实机历史下载失败与ETF期权无报价分别记录在LIVE_ACCEPTANCE.md。新桥部署后，源代码又补充了policy.py的批量参数类型检查、symbols.py的合约子类判定；这两项尚未重新复制到运行中的终端。宿主SDK/hub使用新校验，合约子类判定在宿主同步器执行；在用桥的新接口已完成上述实机检查。下次正常退出QMT后执行prepare即可使文件完全一致，未在线覆盖终端文件。

## 本轮闭环修复

目录与选择器阶段：完整测试`142 passed in 52.26s`，0失败、0跳过。新增目录发现、缺名称partial、重复创建去重、批次检查点恢复与取消、跨1000条分页；浏览器覆盖单选、多选、本页全选、跨页保留、仅看已选、取消不修改原表单、指数名称/板块关联、搜索有内容时Esc关闭和焦点返回。既有管道、下载、存储、导出、认证与桌面/手机/平板回归保持通过。

实机首次浏览器检查暴露搜索框消费Esc的问题，已修复并补回归；后一次实机检查被PostgreSQL磁盘写满打断，没有将数据库异常归因于选择器。用户重建正确数据卷后，从停止前的恢复副本只还原本项目schema，11张表逐行比对一致，目录任务从4736恢复。详见POSTGRES_RECOVERY.md。

恢复后的真实浏览器名称搜索、指数单选、精确板块关联、带搜索文字的Esc关闭、桌面/手机尺寸与终端历史诊断通过。历史诊断提取16:35:34平安银行日线空结果，只展示时间、代码、周期及允许字段；不暴露原始终端日志或推断当前登录状态。历史回补仍未通过。

真实目录任务`9007896d-5e2c-4496-a760-f41da3f4a27b`已完成：检查点7905/7905、保存7905条、missing为空；指数611个、A股5567个、ETF1727个。逐页收集API全部结果，与数据库代码/名称/类别逐条一致，并与任务发现的目录代码/类别一致。沪深300、平安银行、沪深300ETF华泰柏瑞名称与QMT实时资料一致，510300.SH旧误分类已纠正为ETF。

完成后的桌面/手机实机检查覆盖ETF名称与类别筛选、行情复选、数据集跨搜索复选、历史和指数单选、板块关联、Esc与焦点返回，无页面错误。临时验收脚本曾在异步加载结束前读取上一次控件类型而失败；改为等待目标证券代码和控件类型后通过，没有修改产品实现或弱化断言。截图位于output/playwright/live-catalog-*.png；未创建额外数据集或历史下载任务。

## 前一轮历史与订阅修复

完整结果：`128 passed in 37.38s`，0失败、0跳过。测试包含独立PostgreSQL14、真实Windows管道与Chromium，模拟行情只写入隔离schema。

- 分层诊断：桥连通不代表数据就绪；日线、快照时间与交易日历分别显示，接口需认证，探测不下载、不写入业务库，不泄露驱动错误。
- 任务：历史和日历双空时停止，不推进当前检查点；恢复后重试仍包含失败分块。仅行情为空时保留partial；调度日历空、请求异常或不足五日均显示原因，恢复后不重复建任务。
- 部署：PowerShell输出显式UTF-8，中文目录精确匹配进程可执行路径；无法核实进程时阻止部署，不误判终端已退出。
- 闭环工具：三证券日线下载、重复回补、SDK每页2条、数据库与CSV/Parquet逐字段精确比较；拒绝活动任务和非法日期，部分完成不标记通过。
- 浏览器：新增诊断按钮和四项结果，桌面、手机和平板无横向页面溢出或JavaScript错误。图表仍使用隔离库合成数据做canvas像素检查。
- 队列：下载和导出使用独立后台队列及独立 advisory lock；QMT历史或日历请求阻塞时，CSV/Parquet仍可读取已入库数据。任务页分别展示行情调度和文件导出错误。

本轮先用两个阻塞场景复现导出一直排队，再完成修复；另验证导出重启恢复不会重置下载任务，空结果导出会清零旧行数，两个队列各自互斥。真实网页三证券行情时间持续更新、桌面/手机任务页状态检查均通过。

后续订阅回归先复现切换后收到旧证券回调及非法消息断开WebSocket。修复后覆盖停止订阅仍接收任务事件、退订失败保留原订阅、首包、桥标识变化重订阅和连接关闭清理；浏览器覆盖切换、停止、再订阅和WebSocket重连。所有模拟价格仍仅来自隔离测试环境。

收盘后的真实浏览器检查首次因15秒内没有收到三证券新推送而失败，没有将快照当作实时事件补入该断言。单独执行订阅确认、切换ETF、停止、再订阅、WebSocket断线重连及快照按钮检查通过；桌面/手机截图位于output/playwright/live-subscription-flow-*.png。收盘后新推送与QMT终端重启后的行情恢复仍未验证，盘中推送沿用14:04实机记录。

首次测试启动命令遗漏容器默认用户名，导致数据库认证setup错误；修正测试DSN构造后完整通过，未跳过或弱化测试。

实机已确认桥、快照、实时Tick/指定三证券全推行情和PostgreSQL连接。9月16日历史请求有 `200001 / connection is not login`，9月17日14:03短区间历史请求仍报错且读回0条，当前错误码未明确。真实日线、分钟、复权因子和历史文件一致性尚未通过，不能以自动化结果替代。详细记录见LIVE_ACCEPTANCE.md。

## 统一配置阶段记录

新增根目录config.toml，迁移旧JSON中的API Key并保持不变。配置覆盖顺序、独立路径、类型校验、注释保留、环境值不落盘、外部编辑防覆盖、网页写回和SDK共享配置均已测试。

该阶段完整结果为`102 passed in 15.39s`，0失败、0跳过。初轮暴露Path类型运行目录被严格校验拒绝，已修复并新增回归，未跳过失败测试。

用户指定的本机PostgreSQL16.15已连接。检查发现pfor_qmt数据库尚不存在，已创建指定数据库并迁移同名schema，版本1，共10张表；行情表0行。未向业务库写入测试数据，自动化测试仍在独立PostgreSQL14容器中运行。

工作台从config.toml读取真实连接。数据库凭据未写入源码、日志、测试或发布包；原settings.local.json仅作为本地备份保留。该阶段尚未配置QMT，后续实机进展见本轮记录。

## 离线与隔离环境通过

- 完整测试：`78 passed in 12.66s`，0失败、0跳过。设置了隔离测试DSN和浏览器开关。随后中文密码处理和请求超时参数校验调整的相关回归为`27 passed in 3.47s`。
- PostgreSQL：独立临时`postgres:14`容器，本机端口55436；每个测试创建独立`pfor_qmt_test_*` schema并清理。未使用其他应用数据库。
- 上游：固定协议相同输入对比、提取行情方法参数/结果对比、迁移纯JSON回归测试；特殊数值、缺失值、DataFrame和大整数。
- 行情边界：SDK/hub/桥端拒绝交易与超范围操作；历史读取关闭订阅与补值；旧下载回退全部证券；内部TypeError不误判签名缺失；首包、归属、退订失败保留。
- Windows真实命名管道：本机hub、模拟QMT桥、SDK收发，客户端断线重连，绕过SDK的交易请求在hub拒绝。这不是实际QMT终端。
- 数据与任务：迁移幂等、schema边界、精确NUMERIC、NULL、重复入库修订、取消后不写入、检查点续跑、空表partial、下载拒绝不读取旧缓存冒充成功、网络3次重试、能力错误不重试、调度17:00与去重、来源互斥、指数数据集固定成员；导出队列不重置或改写下载任务。
- 导出：CSV UTF-8 BOM、Parquet精确十进制文本、NULL与口径JSON、QMT离线时导出、数据一致性；5002行跨5000行分页的两种格式均验证无丢失。
- HTTP/SDK：认证、无凭据披露、外部Origin拒绝、网页登录Cookie、中文密码、可选密码启停、数据集/任务/取消/重试往返；hub尊重180秒下载请求超时。
- WebSocket：一次性票据、失效票据拒绝、任务事件推送。
- 部署：合成QMT目录不覆盖其他模型，同名未托管模型拒绝、运行中拒绝、XML并发变更拒绝、重复启用无重复节点；内嵌代码Python3.6语法解析，定时回调无参数及原生定时器ID处理。

## 浏览器通过

Chromium真实浏览器，1440×980桌面、390×844手机、768×1024平板。覆盖登录/退出、历史查询、K线canvas非空像素、数据集创建、CSV后台导出与下载，以及五个页面的视口与横向溢出检查。无JavaScript页面错误。

截图位于本地`output/playwright/`，不提交，不进入发布包。截图中的K线来自隔离测试库的合成数据，未注入用户运行数据库。

## 构建通过

`python -m build`生成wheel和sdist；`tools/verify_packages.py`检查迁移SQL、QMT入口、本地ECharts/Lucide、MIT/Apache/BSD/ISC相关许可与NOTICE。包中无runtime、log、exports、output、本地连接配置或虚拟环境。`pip check`通过，JavaScript语法检查通过。

Python依赖固定在`requirements.lock`，构建依赖固定在pyproject.toml；前端资源版本和SHA-256保存在`docs/assets-manifest.json`。

## 实机验收边界

用户PostgreSQL连接和迁移已验证；QMT根目录已配置，自有PFOR_MARKET行情桥运行，快照、板块与证券资料可读，实时Tick/指定三证券全推行情小样本通过。历史任务实际返回空表，历史连接或权限仍待终端侧检查。本轮只重启本项目服务，未修改终端配置，没有账户交易行为。

真实日/分钟线、复权因子、历史权限、单位与时间标签、终端RZRK导入及自动运行、实际17:00周期、长期运行和大规模回补性能均未验收。实时Tick和全市场推送仅通过本次短观察窗口，仍需重连恢复验收。分钟内覆盖保留待核验状态。执行次序见`LIVE_ACCEPTANCE.md`，先指数/股票/ETF小样本，再扩大数据量。

## 当前失败

自动化检查没有失败。真实历史读取与下载未通过：日线和日历为空，长任务已明确失败并保留检查点；实时推送小样本已通过。分钟覆盖、复权因子、真实历史文件对比及实际调度等尚未验证。
