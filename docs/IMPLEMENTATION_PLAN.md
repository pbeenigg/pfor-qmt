# 第一期实施计划

## 历史汇总与资料名称（2026-09-20）

历史行情上方汇总改为可搜索、分页表格；图表按汇总行选择。主力映射和结算参数的图表、表格、详情使用同源证券名称并保留代码辅助识别。未改变行情数值、资料字段或schema，验证见CONSOLE_USABILITY.md与VERIFICATION.md。

## 资料明细查询闭环（2026-09-20）

补齐仓单按仓库/单位、排名按会员的多选筛选；选项从实际入库记录生成，保留原值与NULL。查询、统计、图表和双格式导出复用过滤条件；采集确认明确完整资料范围，查询条件不伪装为上游能力。沿用schema10与既有任务、导出链路，验证记录见VERIFICATION.md。

## 查询上下文与任务定位（2026-09-20）

继续检查列表实际流程：修正排序/页大小隐式提交筛选、期货资料重置及异步选项恢复、详情返回路径；增加任务分块和事件的检索、分页计数和刷新。历史/资料翻页保留图表，图表故障与表格成功分别报告，已选任务状态及断库恢复按实际结果更新。沿用schema10，无数据迁移；检查记录见CONSOLE_USABILITY.md。

## 列表回收站与筛选补齐（2026-09-20）

数据集、维护计划、任务和导出记录增加可恢复回收站；活动状态、配置引用、并发版本及入队均校验。恢复配置保持停用，保留原行情、文件和质量证据。补齐账号/指数列表筛选、已保存查询删除与覆盖确认、末页回退和异步刷新顺序。schema10已备份升级，原20张表原字段核对不变；实际验证见VERIFICATION.md，操作边界见CONSOLE_USABILITY.md。

## 分钟成员与列表易用性（2026-09-20）

分钟数据集按具体月份合约选择与全选，改周期时显式处理不兼容成员。任务列表补齐范围名称、账号、触发方式和日期等上下文；筛选草稿与已应用结果分离，分页/全选/自动刷新保持一致，后台加载不覆盖手动查询。列显隐、日志恢复、窄屏按钮及编辑表单完成修正。逐页评估见CONSOLE_USABILITY.md，完整367项与最终表单专项11项分别记录于VERIFICATION.md；保留上游分钟权限与QMT历史实机边界。

## 维护执行闭环补齐（2026-09-20）

复核确认旧任务派生维护、端点绑定和执行日期还有缺口。本轮补齐：原任务快照及账号保留；名称修改/停用/复制不隐式重绑；明确重新绑定操作；无日历时自定义日期预览；确认执行前重查权限；三种编辑退出方式统一确认；图表严格使用已应用的周期及日期范围。沿用schema9与原有任务、入库、核验及导出链路，不自动重跑旧失败或待核验任务。

## Web 控制台整改（2026-09-18确认，2026-09-19交付）

确认计划保存在pfor_qmt/docs/plans/feat-plan-20260918.md。已完成分组独立页面、查询偏好与排序分页、配置版本化编辑、维护预览执行、任务详情、历史量仓及资料图表、全范围统计和质量下钻。资料页面固定单一类型，各类型内保留多对象批量；HTTP多资料批量接口仍兼容。指南见CONSOLE_GUIDE.md，实际验证与实机边界见VERIFICATION.md。

## 全链路稳健性整改（2026-09-18）

执行状态、质量状态、分块隔离、定向重试、持久化日志与自动维护的验收清单见 [RELIABILITY_PLAN.md](RELIABILITY_PLAN.md)。保留既有数据与任务，不自动触发历史失败任务重跑。

## 批量操作与工作台交互（2026-09-18）

用户追加全选、多选、快捷日期及记录详情。证券类别/交易所/合约类型、历史周期、下载数据集、资料类型/交易所/产品/映射合约、任务状态/类型/来源提供搜索多选；Tushare目录可选择同步交易所。来源和采集账号保持单选，诊断与单指数映射保留单对象语义。

同一快照全选匹配目录，跨页保留，超过10000项明确拒绝；QMT行情选择仍最多100项。批量回补、同步和导出先展示范围，再全部校验、事务创建原有任务，按来源串行采集，不增加第二套队列或表。下载周期按各数据集与用户选择的交集执行，无交集则整批拒绝。

快捷范围为今天、近三天/一周、一/三/六个月、一/二/三年；采用上海日期和日历月末规则。结果按资料类型分页展示，行情表明确证券与周期，图表独立选取单一序列。详情提供字段、原始记录、复制及本页上一条/下一条，任务显示覆盖与错误。验收覆盖隔离数据库、真实浏览器及现有业务样本，见VERIFICATION.md。本次不变更schema或终端模型。

## 期货资料扩展（2026-09-18）

用户追加周/月线、15/30/60分钟、独立日历和主力映射、仓单及成交持仓排名。schema5扩展现有K线与日历表，并在同一schema增加两张明细表；继续使用原账号、download/export任务、查询和文件链路。详细边界见[Tushare扩展](TUSHARE_EXTENSIONS.md)，验收结果见VERIFICATION.md。此项授权取代下文旧范围中对这些研究资料的排除；交易及连续分钟合成仍不纳入。Tick没有官方API，暂不接入自动获取。

开发、schema5迁移和239项完整回归已完成，真实账号小样本入库、SDK/数据库/文件一致性和桌面/手机页面通过。分钟权限拒绝、能源中心原油排名空响应、实际长期调度仍未验收；资料当前仅手动同步。原全市场日线任务在SCTAS.INE全空OHLC处失败，保留已入库记录和1669检查点，详见当前验证记录。

## Tushare 期货接入（2026-09-17 初次验收记录）

1. 统一 config.toml 增加多账号、端点、Token、限流与分接口能力检测，网页写回同一配置且不回传凭据。
2. schema 版本4增加稳定合约ID和来源映射，同一K线表按合约/来源/周期/时间去重；旧数据保留QMT来源与原始单位。
3. 复用目录、任务、校验、入库、查询和导出；Tushare 接入 fut_basic、fut_trade_cal、fut_mapping、fut_daily、ft_mins，分钟仅具体合约。
4. 来源独立队列，账号固定、共享Token限流、截断检测、重启恢复和取消；新Tushare数据集19:00更新，QMT17:00，均回读五个交易日。
5. 工作台和DataClient提供明确来源选择；已入库数据查询和导出不依赖在线源。测试覆盖双源隔离、单位精度、权限、分页、任务和浏览器完整流程。

不接入Tushare实时行情、连续分钟拼接或研究数据。不自动切源或混源。Token由本地录入，实机验证与模拟测试分别报告。详细接口与配置以API.md和CONFIGURATION.md为准。

上述开发及离线验证已完成，197项完整回归通过。业务库已备份并迁移至版本4，8766服务已更新；目录、数据集、任务数量保持一致。真实Tushare调用未执行，需本地配置Token后从小样本开始。详见VERIFICATION.md。

2026-09-17追加范围：用户要求股票、行业概念、指数、期货、期权、场内基金、债券全部纳入，期货/期权优先。以下保留原阶段记录；当前扩展范围与验收见 [七类接入计划](MULTI_ASSET_PLAN.md)，现行规则以AGENTS.md和该扩展计划为准。

## 实施状态（2026-09-17）

规范已首先写入并单独提交。固定来源迁入、纯行情桥、SDK、PostgreSQL迁移、持久化任务、Web五个页面、导出、独立部署向导和打包已实现。本轮完整自动化验证142项通过，包括隔离PostgreSQL、真实本机管道和Chromium。

统一配置已接通用户本机PostgreSQL16.15并初始化指定数据库与schema。2026-09-17 已确认行情桥、盘中快照、单证券Tick和指定三证券的全推行情订阅可读，退订后观察窗口无继续回调；历史数据和交易日历仍为空。旧日志有 `200001 / connection is not login`，今天14:03的日线请求仍报错并读回0条，当前具体连接或权限原因尚待终端侧确认。

已增加只读分层诊断、历史和日历双空时停止任务、可见调度错误、三证券日线闭环验收工具，并修复中文QMT目录的进程识别。下载与文件导出已拆为独立后台队列，历史请求阻塞时仍可导出已入库数据。下一步由用户恢复终端历史行情连接，先完成日线小样本重复下载/入库/分页/文件对比，再验收分钟、复权因子、实际调度和QMT离线查询。真实行情完整闭环尚未通过；详见VERIFICATION.md与LIVE_ACCEPTANCE.md。

网页行情已补齐停止、切换、订阅确认和断线恢复；停止行情不影响任务推送，旧回调不会混入新证券列表。2026-09-17 15:14另以平安银行2025年1月短区间排查：默认下载请求返回true，实际读回0条，历史故障并非仅限最近日期。第5阶段实机验收仍部分完成，不据此扩大回补范围。

证券目录已在真实QMT完成同步：指数611个、A股5567个、ETF1727个，共7905个，名称无缺失。目录任务检查点7905/7905；全部API分页与数据库的代码、名称、类别逐条一致。行情及数据集复选、历史及指数单选、板块筛选下拉已通过实机桌面/手机检查。数据库schema版本2；用户重建PostgreSQL后已从恢复副本还原本项目记录，目录任务从4736续跑完成，详见POSTGRES_RECOVERY.md。历史库仍没有K线，本轮目录完成不代表历史回补验收通过。

## 已确认范围

2026-09-17补充易用性：目录按指数/A股/ETF从QMT全量同步，名称不再要求手填；行情/数据集支持搜索复选，历史/指数使用搜索单选，板块使用筛选下拉。目录同步为持久化任务，迁移至schema版本2；历史错误增加具体证券、周期、日期和脱敏终端历史结果。目录通过不替代历史回补验收。

基于 cfquant 提交 `5baa4daf8dab01fb415afdd45a72cd254cea042f` 派生，仅提供行情。
单 QMT 通用模式，国内指数、沪深北 A 股、ETF；实时快照和订阅，日线及 1/5 分钟线，当前指数成分。
独立 PostgreSQL schema 存不复权行情、复权因子、资料、数据集、任务；CSV/Parquet 导出。
Web 工作台、Python SDK、API Key 与可选网页登录。明确排除交易、多终端、高级模式、LITE、Level2、千档、财务、公式、权重和历史成分、自动登录、在线更新。

## 行为约定

- 首次任务默认日线一年、分钟90天，范围可修改，不自动下载全市场。
- 按指数创建的数据集固定创建时成分，手动刷新成员。
- 按数据集启用交易日17:00更新，Asia/Shanghai；回读最近5个交易日，恢复时补执行到期任务。
- 单源单补数任务；分证券/时间段检查点，网络失败至多重试3次；取消只停止后续分段。下载和导出使用独立队列，导出不被QMT请求阻塞。
- 下载、读缓存、校验、事务入库分别有明确状态；空数据和覆盖缺口不判完整成功。
- 历史库无需QMT在线；实时Tick不持续落库；导出采用不复权口径并带说明文件。
- 本机HTTP默认8766，独立命名管道和QMT模型。数据库使用用户指定实例，凭据仅在本地配置。

## 阶段验收

2026-09-20控制台修复：全局来源跨页与刷新保持，运维导航与统计加载分离，健康/任务日志避免重复读取大型任务内容，数据集图标操作适配桌面与手机。24页实机只读检查及双来源四视口通过；全量361项、后续日志专项30项分别记录于VERIFICATION.md。

用户新增结算参数、成交持仓排名和品种交易周报：新增settle/weekly_detail资源，复用已实现holding。实施范围包括schema8新增资料表、1600/4000行分块、费率原值、周报亿元转元、周日期口径、UI多选和详情、任务维护、核验及双格式导出；权限和实际覆盖单独验收。详见SETTLEMENT_WEEKLY.md。

核验补数续步：明确区分只读重试与真实采集；只对核验确认的具体日期生成补数，预览确认固定来源、账号与端点，保留原任务。完成后可重新核验，关联任务双向导航。schema7与统一任务/行情体系保持，不扩大维护范围。

自动维护续步：统一数据集与维护范围调度，支持编辑、双路径互斥、幂等启停及并发停用保护；逐市场恢复并兼容旧任务日期键。日历权限错误停止重复请求，配置修正或显式重新检测后恢复；不新增来源或扩大证券范围。详见MAINTENANCE.md。

2026-09-18核验续步：schema7允许独立只读核验任务；覆盖QMT/Tushare已有行情与资料，核验不改写原任务或行情。完整日历、周/月修订、目录证据、补数子任务链和断库运行日志已接入；298项完整回归通过，跨日重启用历史时钟回放。真实分钟与夜盘、资料发布时间和长期实机运行继续按QUALITY_VERIFICATION.md列出的外部条件验收，不宣称已经全部通过。

2026-09-18续步：已接入维护范围新鲜度的只读API、SDK与运行面板，按合约/周期分页，缺少日历或发布规则明确待核验；已完成schema5备份独立恢复及schema6升级保值验证。具体判定和剩余实机边界见FRESHNESS.md与RELIABILITY_PLAN.md。

1. 规范与来源：AGENTS、许可证、来源文件哈希、兼容矩阵、独立Git基线。
2. 行情桥与SDK：上游协议对比、交易拒绝、生命周期、首包与退订、旧下载接口全证券回退。
3. PostgreSQL与任务：迁移、去重和修订、精度、恢复、取消重试、调度去重和导出一致性。
4. Web：查询、图表、任务、设置、桌面与移动浏览器验证。
5. 部署与实机：隔离模型、不覆盖原配置；用户指定QMT和数据库的小样本对比；逐项报告通过、失败和未验证，行情连接恢复前不扩大回补。
6. 打包：包含许可与静态资源，排除凭据、运行数据、日志和测试导出。
